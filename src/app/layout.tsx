import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { AuthProvider } from "@/components/auth-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SALES OS — Tableau de bord",
  description: "SALES OS MVP V1 — Multi-tenant sales management platform",
  keywords: ["SALES OS", "Next.js", "TypeScript", "Tailwind CSS", "shadcn/ui", "sales management"],
  authors: [{ name: "SALES OS Team" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "SALES OS",
    description: "Multi-tenant sales management platform",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "SALES OS",
    description: "Multi-tenant sales management platform",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <AuthProvider>
          {children}
        </AuthProvider>
        <Toaster />
      </body>
    </html>
  );
}
